# =============================================================================
# Stock Sentiment Analysis Dashboard - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control - it contains sensitive information
# =============================================================================

# -----------------------------------------------------------------------------
# Azure OpenAI Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Get these values from your Azure OpenAI resource in Azure Portal
# Endpoint: https://your-resource-name.openai.azure.com
# API Key: Found in "Keys and Endpoint" section
# Deployment Name: The name of your GPT-4 deployment
# Embedding Deployment: The name of your embedding model deployment (optional for RAG)

AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com
AZURE_OPENAI_API_KEY=your-azure-openai-api-key-here
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4
AZURE_OPENAI_API_VERSION=2023-05-15
AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-ada-002

# -----------------------------------------------------------------------------
# Redis Cache Configuration (OPTIONAL but RECOMMENDED)
# -----------------------------------------------------------------------------
# Redis is used for caching API responses and storing RAG embeddings
# If not configured, the app will work but without caching
# Get these values from your Azure Cache for Redis resource

REDIS_HOST=your-redis-host.redis.cache.windows.net
REDIS_PORT=6380
REDIS_PASSWORD=your-redis-password-here
REDIS_SSL=true

# Alternative: Use connection string instead of individual settings
# REDIS_CONNECTION_STRING=rediss://:password@host:port

# -----------------------------------------------------------------------------
# Application Settings (OPTIONAL - defaults provided)
# -----------------------------------------------------------------------------

# Logging Configuration
APP_LOG_LEVEL=INFO                    # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
APP_DEBUG=false                       # Enable debug mode (true/false)

# Cache TTL Settings (in seconds)
APP_CACHE_TTL_SENTIMENT=86400         # Sentiment cache TTL: 24 hours
APP_CACHE_TTL_STOCK=3600              # Stock data cache TTL: 1 hour
APP_CACHE_TTL_NEWS=7200               # News cache TTL: 2 hours

# RAG (Retrieval Augmented Generation) Settings
APP_RAG_TOP_K=3                       # Number of similar articles to retrieve for context
APP_RAG_SIMILARITY_THRESHOLD=0.3      # Minimum similarity score (0.0-1.0) for article retrieval

# =============================================================================
# Configuration Notes:
# =============================================================================
#
# 1. Azure OpenAI is REQUIRED for sentiment analysis
#    - Create an Azure OpenAI resource in Azure Portal
#    - Deploy a GPT-4 model (or GPT-3.5-turbo)
#    - For RAG, also deploy an embedding model (e.g., text-embedding-ada-002)
#
# 2. Redis is OPTIONAL but HIGHLY RECOMMENDED
#    - Significantly improves performance through caching
#    - Required for RAG functionality (stores article embeddings)
#    - Can use Azure Cache for Redis or any Redis instance
#
# 3. Application settings have sensible defaults
#    - Only override if you need different behavior
#    - Cache TTLs balance freshness vs. API costs
#    - RAG settings control context quality vs. quantity
#
# 4. Security Best Practices:
#    - Never commit .env file to version control
#    - Use Azure Key Vault for production deployments
#    - Rotate API keys regularly
#    - Use least-privilege access for Azure resources
#
# =============================================================================
