# =============================================================================
# Stock Sentiment Analysis Dashboard - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values
# cp .env.example .env
# =============================================================================

# =============================================================================
# REQUIRED: Azure OpenAI Configuration
# =============================================================================
# Get these from Azure Portal: https://portal.azure.com -> Your Azure OpenAI resource

AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_KEY=your-azure-openai-api-key-here
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4
AZURE_OPENAI_API_VERSION=2023-05-15
AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-ada-002


# =============================================================================
# OPTIONAL: Redis Cache (Recommended for performance)
# =============================================================================
# Redis is optional but recommended. Without it, the app works but without caching.

REDIS_HOST=your-redis-host.redis.cache.windows.net
REDIS_PORT=6380
REDIS_PASSWORD=your-redis-password-here
REDIS_SSL=true

# Alternative: Use connection string instead
# REDIS_CONNECTION_STRING=rediss://:password@host:6380/0


# =============================================================================
# OPTIONAL: Azure AI Search (Recommended for RAG)
# =============================================================================
# Azure AI Search is optional but recommended for RAG. Without it, uses Redis (slower).

AZURE_AI_SEARCH_ENDPOINT=https://your-search.search.windows.net
AZURE_AI_SEARCH_API_KEY=your-azure-ai-search-api-key-here
AZURE_AI_SEARCH_INDEX_NAME=stock-articles


# =============================================================================
# OPTIONAL: Additional Data Sources
# =============================================================================
# yfinance is always enabled (no API key needed). Enable these for more coverage.

# Reddit (Free, register at https://www.reddit.com/prefs/apps)
DATA_SOURCE_REDDIT_CLIENT_ID=
DATA_SOURCE_REDDIT_CLIENT_SECRET=
DATA_SOURCE_REDDIT_ENABLED=false

# Alpha Vantage (Free tier: 500 calls/day, get key at https://www.alphavantage.co/support/#api-key)
DATA_SOURCE_ALPHA_VANTAGE_API_KEY=
DATA_SOURCE_ALPHA_VANTAGE_ENABLED=false

# Finnhub (Free tier: 60 calls/minute, get key at https://finnhub.io/register)
DATA_SOURCE_FINNHUB_API_KEY=
DATA_SOURCE_FINNHUB_ENABLED=false
